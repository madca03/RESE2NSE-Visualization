1. Create "graph" database in mysql
2. Run "npm run setupdb" to generate the database tables and seed data
3. Run "npm run archivedb" (for the python archiver module)
4. Run "npm start" to start the server
